-- Q12. Event with Maximum Sessions: List the event(s) with the highest number of sessions.

select e.title
from events e
join sessions s on s.event_id=e.event_id
having s.MAX(TIMESTAMPDIFF(MINUTE,start_date,end_date));
